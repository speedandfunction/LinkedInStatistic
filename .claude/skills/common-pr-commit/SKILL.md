---
name: common-pr-commit
description: Commit, push, and update the PR. Use when the user says "commit", "push", "commit and push", or similar.
---

# PR Commit

1. Invoke the `common-update-memory` skill.
2. Run `./.claude/skills/common-pr-commit/commit.sh`.
3. Invoke the `common-pr-update` skill.
