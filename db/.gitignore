# Дамп бази — це весь корпус одним файлом: імена, хедлайни, тексти коментарів.
# backup.sh пише туди, куди скажуть; якщо сказали «сюди» — git цього бачити не має.
# (schema.sql — єдиний .sql, якому тут місце.)
*.sql
!schema.sql
*.sql.partial.*
*.dump
backups/
